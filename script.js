// script.js - JavaScript functionalities for the Valentine website\n\n// Password verification function\nfunction verifyPassword(password) {\n    const minLength = 8;\n    const hasUpperCase = /[A-Z]/.test(password);\n    const hasLowerCase = /[a-z]/.test(password);\n    const hasNumbers = /[0-9]/.test(password);\n    const hasSpecialChars = /[!@#$%^&*()_+\-=\[\]{};':\\|,.<>\/?]/.test(password);\n\n    if (password.length < minLength) {\n        return false; // Password too short\n    }\n    return hasUpperCase && hasLowerCase && hasNumbers && hasSpecialChars;\n}\n\n// Audio persistence across pages\nlet audio;\nwindow.addEventListener('load', () => {\n    audio = new Audio('path/to/audio.mp3');\n    if (localStorage.getItem('isPlaying') === 'true') {\n        audio.play();\n    }\n});\n\nwindow.addEventListener('beforeunload', () => {\n    localStorage.setItem('isPlaying', 'true');\n});\n\n// Image gallery navigation\nlet currentIndex = 0;\nconst images = document.querySelectorAll('.gallery img');\n\nfunction showImage(index) {\n    images.forEach((img, i) => {\n        img.style.display = (i === index) ? 'block' : 'none';\n    });\n}\n\nfunction nextImage() {\n    currentIndex = (currentIndex + 1) % images.length;\n    showImage(currentIndex);\n}\n\nfunction previousImage() {\n    currentIndex = (currentIndex - 1 + images.length) % images.length;\n    showImage(currentIndex);\n}\n\n// Page transitions\nfunction smoothTransition(newPage) {\n    document.body.classList.add('fade-out');\n    setTimeout(() => {\n        window.location.href = newPage;\n    }, 500); // adjust to transition duration\n}\n\ndocument.addEventListener('DOMContentLoaded', () => {\n    showImage(currentIndex);\n    // Event listeners for gallery navigation\n    document.getElementById('nextBtn').addEventListener('click', nextImage);\n    document.getElementById('prevBtn').addEventListener('click', previousImage);\n});